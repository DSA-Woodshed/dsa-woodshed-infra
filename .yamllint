---
# Lint policy for this overlay's YAML: strict on correctness, relaxed on
# style rules that fight GitHub workflow idioms and annotated config files.
extends: default

rules:
  line-length:
    max: 120
  document-start: disable
  truthy:
    allowed-values: ["true", "false", "on", "off"]
  comments-indentation: disable
